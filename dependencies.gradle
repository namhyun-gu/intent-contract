ext.versions = [
        version        : 'git describe --tags'.execute([], project.rootDir).text.split("-")[0],

        gradleBuildTool: '4.0.0',
        bintrayRelease : '0.9.2',
        spotlessGradle : '4.0.1',

        kotlin         : '1.3.72'
]